<Page
    x:Class="MrmTool.PriPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MrmTool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:selectors="using:MrmTool.Selectors"
    xmlns:models="using:MrmTool.Models"
    xmlns:wct="using:CommunityToolkit.WinUI.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Page.Resources>
        <DataTemplate x:Key="TreeTemplate" x:DataType="models:ResourceItem">
            <muxc:TreeViewItem AutomationProperties.Name="{x:Bind DisplayName}"
                          ItemsSource="{x:Bind Children}" IsExpanded="False" Margin="0,2,0,2">
                <StackPanel Orientation="Horizontal">
                    <Image Width="28" Source="{x:Bind Icon}" Margin="0,0,10,0"/>
                    <TextBlock Text="{x:Bind DisplayName}" VerticalAlignment="Center" />
                </StackPanel>
            </muxc:TreeViewItem>
        </DataTemplate>

        <DataTemplate x:Key="ResourceTemplate" x:DataType="models:ResourceItem">
            <muxc:TreeViewItem AutomationProperties.Name="{x:Bind DisplayName}" Margin="0,2,0,2">
                <StackPanel Orientation="Horizontal">
                    <Image Width="28" Source="{x:Bind Icon}" Margin="0,0,10,0"/>
                    <TextBlock Text="{x:Bind DisplayName}" VerticalAlignment="Center" />
                </StackPanel>
            </muxc:TreeViewItem>
        </DataTemplate>

        <selectors:ResourceItemTemplateSelector x:Key="ResourceItemTemplateSelector"
            ResourceTree="{StaticResource TreeTemplate}"
            Resource="{StaticResource ResourceTemplate}" />

        <SolidColorBrush x:Key="PriBorderBrush" Color="{ThemeResource TextFillColorSecondary}" Opacity="0.4"/>
    </Page.Resources>

    <Grid Background="Transparent" AllowDrop="True" DragOver="Grid_DragOver" Drop="Grid_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <MenuBar Grid.Row="0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="Open" Click="Open_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="OpenFile"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Save" Click="Save_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Save"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Save As" Click="SaveAs_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="SaveLocal"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Exit" Click="Exit_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Cancel"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuBarItem>
            <MenuBarItem Title="Edit">
                <MenuFlyoutItem Text="Add Resource" Click="AddResource_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Add"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="removeResourcesMenuItem" Text="Remove Resource(s)" IsEnabled="False" Click="RemoveResources_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Delete"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Set Root Folder" Click="SetRootFolder_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="OpenLocal"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Embed Path Resources In" Click="EmbedPathResources_Click">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="ImportAll"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuBarItem>
        </MenuBar>
        <Grid Grid.Row="1" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="280"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <muxc:TreeView x:Name="treeView" Grid.Column="0" CanDragItems="False" CanReorderItems="False" ItemsSource="{x:Bind ResourceItems}" ItemTemplateSelector="{StaticResource ResourceItemTemplateSelector}"/>

            <Grid Grid.Column="2" VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="180" MinHeight="130"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="*" MinHeight="130"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Margin="16,0,10,10" BorderThickness="0.5" BorderBrush="{ThemeResource PriBorderBrush}" CornerRadius="8">
                    <ListView>
                        <ListView.Header>
                            <Border Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}">
                                <interactivity:Interaction.Behaviors>
                                    <behaviors:StickyHeaderBehavior/>
                                </interactivity:Interaction.Behaviors>

                                <wct:DataTable Margin="10,0,0,0" Height="30" ColumnSpacing="16">
                                    <wct:DataColumn Content="Type" MinWidth="100" CanResize="True"/>
                                    <wct:DataColumn Content="Qualifiers"/>
                                </wct:DataTable>
                            </Border>
                        </ListView.Header>

                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                            </Style>
                        </ListView.ItemContainerStyle>

                        <ListViewItem>
                            <wct:DataRow HorizontalAlignment="Left">
                                <TextBlock Text="String" VerticalAlignment="Center"/>
                                <TextBlock Text="(Language = en-US, Scale = 200)" VerticalAlignment="Center"/>
                            </wct:DataRow>
                        </ListViewItem>

                        <ListViewItem>
                            <wct:DataRow HorizontalAlignment="Left">
                                <TextBlock Text="Path" VerticalAlignment="Center"/>
                                <TextBlock Text="(Language = fr-FR), (Language = en-US, Fallback Score = 0.5)" VerticalAlignment="Center"/>
                            </wct:DataRow>
                        </ListViewItem>

                        <ListViewItem>
                            <wct:DataRow HorizontalAlignment="Left">
                                <TextBlock Text="Embedded Data" VerticalAlignment="Center"/>
                                <TextBlock Text="(None)" VerticalAlignment="Center"/>
                            </wct:DataRow>
                        </ListViewItem>
                    </ListView>
                </Border>
                <Border Grid.Row="2" Margin="16,0,10,10" BorderThickness="0.5" BorderBrush="{ThemeResource PriBorderBrush}" CornerRadius="8">
                </Border>

                <wct:GridSplitter Grid.Row="1" Height="12" ResizeBehavior="BasedOnAlignment" ResizeDirection="Rows" Background="Transparent">
                    <wct:GridSplitter.Element>
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" IsHitTestVisible="False" Opacity="0.5" VerticalAlignment="Top" HorizontalAlignment="Center" TextAlignment="Center"/>
                    </wct:GridSplitter.Element>
                    <wct:GridSplitter.RenderTransform>
                        <TranslateTransform Y="-5" />
                    </wct:GridSplitter.RenderTransform>
                </wct:GridSplitter>
            </Grid>

            <wct:GridSplitter Grid.Column="1" Width="16" ResizeBehavior="BasedOnAlignment" ResizeDirection="Auto" Background="Transparent">
                <wct:GridSplitter.Element>
                    <Rectangle Fill="{ThemeResource ButtonForegroundThemeBrush}" Width="0.5" Opacity="0.1" VerticalAlignment="Stretch" HorizontalAlignment="Center"/>
                </wct:GridSplitter.Element>
                <wct:GridSplitter.RenderTransform>
                    <TranslateTransform X="8" />
                </wct:GridSplitter.RenderTransform>
            </wct:GridSplitter>
        </Grid>
    </Grid>
</Page>
